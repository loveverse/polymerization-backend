package com.loveverse.wallpaper.utils;

import cn.hutool.core.lang.hash.Hash;

import javax.imageio.ImageIO;
import java.awt.image.BufferedImage;
import java.io.InputStream;

/**
 * @author love
 * @since 2025/4/8
 */
public class ImageHashUtil {
    //private static final PerceptiveHash pHash = new PerceptiveHash(64); // 感知哈希，64位
    //
    //public static String getImageHash(InputStream inputStream) throws Exception {
    //    BufferedImage img = ImageIO.read(inputStream);
    //    Hash hash = pHash.hash(img);
    //    return hash.getHashValue().toString(16);  // 十六进制字符串
    //}
}
